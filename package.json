{
  "name": "mugs",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "pnpm --filter @mugs/web dev",
    "build": "pnpm --filter @mugs/web build",
    "preview": "pnpm --filter @mugs/web preview",
    "core": "pnpm --filter @mugs/core start",
    "migrate": "pnpm --filter @mugs/core migrate"
  }
}
